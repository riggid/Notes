<%*
const subjectFolder = tp.file.folder(true).split('/')[1];
const semesterFolder = tp.file.folder(true).split('/')[0];
const subjectFile = `${semesterFolder}/${subjectFolder}/${subjectFolder}`;
%>
# [[<% subjectFile %>|Back]]
***